import 'package:flutter/material.dart';

class EliteProPage extends StatelessWidget {
  EliteProPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Elite Pro'),
      ),
    );
  }
}
